# This file exists alongside pyproject.toml/uv.lock for deployment
# platforms (e.g. Render) that expect a plain requirements.txt instead of
# reading uv's lockfile. Locally, prefer `uv sync` (see README.md), which
# reads pyproject.toml/uv.lock instead of this file. If you add or upgrade a
# dependency, update both pyproject.toml and this file so they stay in sync.

# --- Web framework ---
fastapi==0.136.3
uvicorn==0.48.0
jinja2==3.1.6

# --- Agent orchestration (LangChain / LangGraph) ---
langgraph==1.2.2
langgraph-checkpoint-postgres==3.1.0
langchain==1.3.2
langchain-community==0.4.2

# --- LLM + search providers ---
langchain-groq==1.1.3
langchain-tavily==0.2.18
tavily-python==0.7.24

# --- Conversation-memory database (PostgreSQL) ---
psycopg[binary]==3.3.4
psycopg_pool==3.3.1

# --- Flight lookup helpers (tools/flight_tool.py) ---
airportsdata==20260315
pycountry==26.2.16
requests==2.34.2

# --- Misc utilities ---
python-dotenv==1.2.2
